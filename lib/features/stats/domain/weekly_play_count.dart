class WeeklyPlayCount {
  final DateTime weekStart;
  final int count;
  final String label;

  const WeeklyPlayCount({
    required this.weekStart,
    required this.count,
    required this.label,
  });
}
